The subdirectory of source called "userini_example" contains test runs and inputs.

Workflow in source: 
> 'make clean' 
> 'make'
> cd userini_example
> mkdir myrundir
> cp ../myfluka myrundir
> cp example.inp myrundir
> cp test.h5m myrundir
> cp runme myrundir
> cd myrundir
> source runme
This creates a fluka_xxx subdirectory that will be deleted if there are no errors.

